Speed up packed-leaf (``leaf_size > 1``) BVH and mesh AABB queries by loading each candidate primitive's bounds up front so the overlap test issues as a single predicate chain, and by routing the primitive reads through the CUDA read-only path (``__ldg``). Up to 21% faster on an L40; singleton-leaf trees and other query kinds are unaffected, and query results are unchanged.
