Speed up BVH and mesh spatial queries by routing primitive-level reads (primitive indices, item/face bounds, and mesh vertices) through the CUDA read-only data path (``__ldg``), and by loading candidate bounds into locals before the AABB overlap test. Packed-leaf AABB queries run up to 21% faster on an L40; query results are unchanged.
