Speed up all four ``wp.Bvh`` query kinds (AABB, ray, sphere, and capsule) and the ``wp.Mesh`` AABB and sphere queries, most notably for a ``wp.Bvh`` built with ``leaf_size > 1`` or a ``wp.Mesh`` built with ``bvh_leaf_size > 1``.
