Speed up packed-leaf (``leaf_size > 1``) BVH and mesh queries by enumerating a leaf's primitives from register-held cursors instead of re-popping and reloading the leaf node once per primitive. Leaf-8 ray and capsule queries run up to ~21% faster and mesh AABB queries 5-15% faster on an L40; query results are unchanged.
