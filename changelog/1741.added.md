Add `wp.bvh_query_sphere()` and `wp.mesh_query_sphere()` for Minkowski-offset broad-phase queries (exact sphere-AABB test, tighter than AABB inflation). Extend `wp.bvh_query_ray()` with an optional `radius` parameter for conservative capsule sweeps over a BVH. Add `exact_filter_triangles` parameter to `wp.mesh_query_aabb()` for exact triangle-vs-box SAT filtering. Add `wp.mesh_get_bvh()` to expose the mesh's internal BVH for direct BVH queries.
