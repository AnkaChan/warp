Share a single shrinking-radius traversal across the mesh closest-point query variants (signed and unsigned ``mesh_query_point*``), so each BVH node is loaded once and pops pruned by the shrinking radius touch no memory. Closest-point queries run 5-19% faster on an L40 and the implementation drops about 500 lines; query results are unchanged.
